/* ================================================== */
/* ==============>> BASE APP JAVASCRIPT <<=========== */
/* ================================================== */


$(document).ready(function() {
    // === ALERTS ===
    $("a.flash-alert__button").click(function(e) {
        $(this).parent(".flash-alert").fadeOut();
        e.preventDefault();
    });
});