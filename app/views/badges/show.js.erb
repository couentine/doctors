updateDynamicColumnContents('experts', '<%= escape_javascript render(@expert_logs, badge: @badge, group: @group, is_admin: @current_user_is_admin) %>');
$('#paginator-experts').html('<%= escape_javascript(paginate(@expert_logs, remote: true, param_name: "pe").to_s) %>');

updateDynamicColumnContents('learners', '<%= escape_javascript render(@learner_logs, badge: @badge, group: @group, is_admin: @current_user_is_admin) %>');
$('#paginator-learners').html('<%= escape_javascript(paginate(@learner_logs, remote: true, param_name: "pl").to_s) %>');