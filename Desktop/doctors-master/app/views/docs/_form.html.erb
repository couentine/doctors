<%= form_with(model: doc, local: true) do |form| %>
  <% if doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doc.errors.count, "error") %> prohibited this doc from being saved:</h2>

      <ul>
      <% doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :doc_name %>
  </div>

  <div class="field">
    <%= form.label :specialty %>
    <%= form.text_field :specialty, id: :doc_specialty %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.text_field :zip, id: :doc_zip %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_field :review, id: :doc_review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
